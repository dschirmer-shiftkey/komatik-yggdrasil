# Cairn — VPS / production environment (STUB)
#
# Copy to infrastructure/.env on the server (never commit):
#   cp infrastructure/.env.production.example infrastructure/.env
#
# Fill every TODO. See docs/deploy/README.md for open decisions.

# --- Required ---

POSTGRES_PASSWORD=TODO-strong-unique-password

# Publisher (required for compose.prod.yaml — not for dry-run)
GITHUB_TOKEN=TODO-ghp-or-fine-grained-token
GITHUB_REPO=KomatikAI/cairn
GITHUB_BRANCH=TODO-main-or-dedicated-branch

SEED_NAME="Homelessness LA"
SEED_DIR=seeds/002-homelessness-la

# LLM — TODO: confirm provider strategy (OpenRouter-only vs multi-provider)
OPENROUTER_API_KEY=TODO
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=

SEED_VIRTUAL_KEY=TODO-virtual-key-matches-bifrost-governance

# Cairn world-tree Supabase (NOT Komatik platform DB)
SUPABASE_URL=TODO-https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=TODO

SOURCE_TYPE=seed
SOURCE_ID=002-homelessness-la
CATEGORY_ID=housing
ROOT_ID=basic-needs

# --- Production tuning (TODO) ---

# CYCLE_INTERVAL_MINUTES=1440
# PUBLISH_INTERVAL_MINUTES=30
# BIFROST_RESET_CONFIG=false

# --- Ports on VPS (TODO: prefer loopback-only in compose.prod.yaml) ---
# GATEWAY_PORT=18789
# BIFROST_PORT=8080
# DB_PORT=5432
